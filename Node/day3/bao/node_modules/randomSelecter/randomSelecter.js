module.exports.randomSelecter = function(arr, num) {
    var newArr = [];
    for (var i = 0; i < num; i++) {
        var index = Math.floor(Math.random() * arr.length);
        console.log(index);
        newArr.push(arr[index]);
        arr.splice(index, 1);
    }
    return newArr;
}